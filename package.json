{
  "name": "contact-page",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel index.html",
    "build": "npx parcel build index.html",
    "deploy": "npx parcel build index.html && netlify deploy",
    "deploy:prod": "npx parcel build index.html && netlify deploy --prod"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@t_g/default-ui": "^0.26.2"
  }
}
