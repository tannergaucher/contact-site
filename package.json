{
  "name": "contact-page",
  "author": "tannermichaelgaucher@gmail.com",
  "version": "1.0.0",
  "description": "tannergaucher.com",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel index.html",
    "build": "npx parcel build index.html",
    "deploy": "npx parcel build index.html && netlify deploy",
    "deploy:prod": "npx parcel build index.html && netlify deploy --prod"
  },
  "license": "ISC",
  "dependencies": {
    "@t_g/default-ui": "^0.29.1"
  },
  "devDependencies": {
    "process": "^0.11.10"
  }
}
